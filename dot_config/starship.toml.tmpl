[directory]
truncation_symbol = "…/"

[git_branch]
symbol = "󰘬 "

[git_commit]
tag_disabled = false

[git_metrics]
disabled = false

[mise]
disabled = false
# `on` is not shown unlike what the documentation says
format = "on [$symbol$health]($style)"
symbol = "🍽️ "

[localip]
disabled = false

{{ if eq .usage "work" -}}
[shell]
disabled = false
powershell_indicator = "Windows PowerShell"
pwsh_indicator = "pwsh"
{{- end }}

[shlvl]
disabled = false
